syntax = "proto3";
package proto.src.grpc.pb;

message FileRequest {
    oneof type {
        bytes buffer = 1;
        string name = 2;
    }
}


message FileResponse {
    string text = 1;
}

service File {
    rpc UploadFile (stream FileRequest) returns (FileResponse) {
    }
}
from random import randint

# chooses von of the values
def react(values):
    index = randint(0, len(values))
    return values[index]


# uses the keyword for a search, e.g. the weather
def search(keyword):
    pass
